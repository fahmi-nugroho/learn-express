import { sum } from '../src/sum.js';

test('1 adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
});

test('2 adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
});

test('3 adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
});